with c;

pub fn panic(s: []u8) never {
    c::fputs(c::to_cstr(s), c::stderr);
    return c::exit(c::EXIT_FAILURE);
}

pub fn assert(cond: bool) {
    if !cond { return panic("Assertion failed\n"); }
}
