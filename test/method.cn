type Vector2 {
    x: i32,
    y: i32,
}

for Vector2 {
    pub fn sum(self: Self) i32 {
        return self.x + self.y;
    }
}

fn main() {
    mut v = Vector2 { x: 1, y: 2 };

    core::assert(v.sum() == 3);
    core::assert(Vector2::sum(v) == 3);
}
