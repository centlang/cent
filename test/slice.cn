with std;

fn main() {
    let array = [5]i32{1, 2, 3, 4, 5};
    mut slice: []i32 = array;

    std::assert(slice.len == 5);
    std::assert(slice[0] == 1);

    slice = slice[1:3];

    std::assert(slice.len == 2);
    std::assert(slice[0] == 2);
}
