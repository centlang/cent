pub fn main() i32 {
    let array = [i32, 5]{1, 2, 3, 4, 5};
    mut slice: [i32] = array;

    assert(slice.len == 5);
    assert(slice[0] == 1);

    slice = slice[1:3];

    assert(slice.len == 2);
    assert(slice[0] == 2);

    return 0;
}
