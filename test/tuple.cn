with c;
with std::mem;

fn main() {
    mut tuple = (1, 3.4, "Hello, world!", false);

    core::assert(tuple.0 == 1);
    core::assert(tuple.1 == 3.4);
    core::assert(mem::equal(tuple.2, "Hello, world!"));
    core::assert(tuple.3 != true);

    mut tuple: (i32, f32, []u8, bool) = (1, 3.4, "Hello, world!" as []u8, false);

    core::assert(tuple.0 == 1);
    core::assert(tuple.1 == 3.4);
    core::assert(mem::equal(tuple.2, "Hello, world!"));
    core::assert(tuple.3 != true);

    tuple.0 = 6;
    tuple.1 = 3.5;
    tuple.2 = "Hi!";
    tuple.3 = !tuple.3;

    core::assert(tuple.0 == 6);
    core::assert(tuple.1 == 3.5);
    core::assert(mem::equal(tuple.2, "Hi!"));
    core::assert(tuple.3 == true);
}
