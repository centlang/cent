type Vector3 {
    x: i32,
    y: i32,
    z: i32,
}

pub fn main() i32 {
    mut v = Vector3 { x: 1, y: 2, z: 3 };

    v.x += 1;
    assert v.x == 2;

    v.y = 3;
    assert v.y == 3;

    v.z = v.x + v.y;
    assert v.z == 5;

    return 0;
}
