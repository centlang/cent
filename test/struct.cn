with std;

type Vector3 {
    x: i32,
    y: i32,
    z: i32,
}

fn main() {
    mut v = Vector3 { x: 1, y: 2, z: 3 };

    v.x += 1;
    std::assert(v.x == 2);

    v.y = 3;
    std::assert(v.y == 3);

    v.z = v.x + v.y;
    std::assert(v.z == 5);
}
