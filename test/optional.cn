fn main() {
    mut a: ?i32 = null;

    core::assert(a == null);
    core::assert((a ?? 7) == 7);

    a = 3;

    core::assert(a.! == 3);
    core::assert((a ?? 7) == 3);
    core::assert(a != null);

    mut a: ? ?i32 = 3 as ?i32;
    core::assert(a.!.! == 3);
    core::assert(a != null);
}
