fn add(a: i32, b: i32) i32 {
    return a + b;
}

fn mul(a: i32, b: i32) i32 {
    return a * b;
}

fn max(a: i32, b: i32) i32 {
    if a >= b {
        return a;
    }

    return b;
}

fn call(func: *fn(a: i32, b: i32) i32, a: i32, b: i32) i32 {
    return func(a, b);
}

fn main() {
    let add_func = &add;

    core::assert(call(&add, 42, 42) == 84);
    core::assert(call(add_func, 42, 42) == 84);
    core::assert(add_func(42, 42) == 84);

    let mul_func = &mul;

    core::assert(call(mul_func, 42, 42) == 1764);
    core::assert(call(&mul, 42, 42) == 1764);
    core::assert(mul_func(42, 42) == 1764);

    let max_func = &max;

    core::assert(call(max_func, -42, 42) == 42);
    core::assert(call(&max, 42, -42) == 42);
    core::assert(max_func(-42, 42) == 42);
}
