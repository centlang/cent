type Id = u16;
type F64 = f64;

#(distinct) type DistinctId = u16;

fn main() {
    let a: Id = 42;
    core::assert(a == 42);

    let b: F64 = 3.5;
    core::assert(b == 3.5);

    let c = 4.5 as F64;
    core::assert(c == 4.5);

    let d = 3 as DistinctId;
    core::assert(d as u16 == 3);
}
