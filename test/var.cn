fn main() {
    mut var: i32;
    core::assert(var == 0);

    let var = 3;
    core::assert(var == 3);

    mut var = var;
    core::assert(var == 3);

    var = 2;
    core::assert(var == 2);

    let var: i32 = var + 3;
    core::assert(var == 5);
}
